import React from "react";
import Connect from "../../components/connect/Connect";

function connexion({ setUserco }) {
  return (
    <div>
      <Connect setUserco={setUserco} />
    </div>
  );
}

export default connexion;
